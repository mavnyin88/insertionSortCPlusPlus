# insertionSortCPlusPlus

- Written in C++
- Program reads in a text file and uses insertion sort algorithm (linked list) to sort in ascending order
- Program outputs sorted list to a text file

# By Michael Avnyin 
